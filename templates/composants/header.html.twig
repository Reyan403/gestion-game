{# ═══════════════════ HEADER ═══════════════════ #}
<header class="fixed top-0 inset-x-0 z-50 bg-[#0a0a0a]/90 backdrop-blur-md border-b border-white/5">
	<div class="max-w-7xl mx-auto px-6 h-[60px] flex items-center justify-between">
		<a href="{{ path('app_home') }}" class="flex items-center gap-2.5 group">
			<span class="font-bold text-[1.05rem] text-white tracking-wide">GameVault</span>
		</a>
		<div class="flex items-center gap-4">
			<button class="text-gray-400 hover:text-white p-2">
				<svg class="w-[18px] h-[18px]" fill="none" stroke="currentColor" viewbox="0 0 24 24"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35" stroke-linecap="round" stroke-width="2"/></svg>
			</button>
			<a href="{{ path('app_login') }}" class="hidden sm:block text-gray-300 hover:text-white text-xs font-medium uppercase tracking-wider">Connexion</a>
		</div>
	</div>
</header>
